#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура НачПериодаПриИзменении(Элемент)
	КонПериода = КонецМесяца(НачПериода);
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура Сформировать(Команда)            
	
	Если НЕ Контрагент.Пустая() Тогда 
	    СформироватьОСВКПоКДНаСервере();
		СформироватьОперацииНаСервере();
	Иначе
		Если НЕ ПарныеСчета Тогда 
			СформироватьОСВКНаСервере();
		Иначе
			СформироватьОСВКПарыНаСервере();
		КонецЕсли;
	КонецЕсли;		
	СформироватьОСВ1549НаСервере();
	СформироватьАвансыНаСервере();
	СформироватьДоотгрузкуНаСервере();
		
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Процедура СформироватьОСВКНаСервере()
	
	ВидыСубконто = Новый СписокЗначений;
	ВидыСубконто.Добавить(ПланыВидовХарактеристик.ВидыСубконтоХозрасчетные.Контрагенты);
	ВидыСубконто.Добавить(ПланыВидовХарактеристик.ВидыСубконтоХозрасчетные.Договоры);
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ОСВ.Субконто1 КАК Контрагент,
	|	ОСВ.Субконто2 КАК Договор,
	|	ОСВ.Счет КАК Счет,
	|	ОСВ.СуммаОборотДт КАК Дебит,
	|	ОСВ.СуммаОборотКт КАК Кредит,
	|	ОСВ.СуммаНачальныйОстатокДт - ОСВ.СуммаНачальныйОстатокКт КАК СальдоН,
	|	ОСВ.СуммаКонечныйОстатокДт - ОСВ.СуммаКонечныйОстатокКт КАК СальдоК
	|ИЗ
	|	РегистрБухгалтерии.Хозрасчетный.ОстаткиИОбороты(&НачПериода, &КонПериода, , , , &ВидыСубконто, ) КАК ОСВ
	|
	|УПОРЯДОЧИТЬ ПО
	|	Контрагент,
	|	Договор,
	|	Счет
	|АВТОУПОРЯДОЧИВАНИЕ";
	
	Запрос.УстановитьПараметр("НачПериода", НачПериода);
	Запрос.УстановитьПараметр("КонПериода", КонецДня(КонПериода));
	Запрос.УстановитьПараметр("ВидыСубконто", ВидыСубконто);
	
	ТЗ = Запрос.Выполнить().Выгрузить();
		
	Объект.ОСВК.Загрузить(ТЗ);
	
	//Для Каждого СТЧ Из Объект.ОСВК Цикл
	//	
	//	СТЧ.СальдоНДт 	= СТЧ.СальдоНДт - СТЧ.СальдоНКт;
	//	СТЧ.СальдоНКт 	= 0;
	//	Если СТЧ.СальдоНДт < 0 Тогда 
	//		СТЧ.СальдоНКт 	= - СТЧ.СальдоНДт;
	//		СТЧ.СальдоНДт 	= 0;
	//	КонецЕсли;			
	//	
	//	СТЧ.СальдоКДт 	= СТЧ.СальдоНДт - СТЧ.СальдоНКт + СТЧ.Дебит - СТЧ.Кредит;
	//	СТЧ.СальдоККт 	= 0;
	//	Если СТЧ.СальдоКДт < 0 Тогда 
	//		СТЧ.СальдоККт 	= - СТЧ.СальдоКДт;
	//		СТЧ.СальдоКДт 	= 0;
	//	КонецЕсли;			
	//	
	//КонецЦикла;
	
	
КонецПроцедуры

&НаСервере
Процедура СформироватьОСВКПарыНаСервере()
	
	ВидыСубконто = Новый СписокЗначений;
	ВидыСубконто.Добавить(ПланыВидовХарактеристик.ВидыСубконтоХозрасчетные.Контрагенты);
	ВидыСубконто.Добавить(ПланыВидовХарактеристик.ВидыСубконтоХозрасчетные.Договоры);
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	Хозрасчетный.Ссылка КАК Счет,
	|	Хозрасчетный.ПарныйСчет
	|ПОМЕСТИТЬ ВременнаяТаблицаПары
	|ИЗ
	|	ПланСчетов.Хозрасчетный КАК Хозрасчетный
	|ГДЕ
	|	Хозрасчетный.ПарныйСчет <> ЗНАЧЕНИЕ(ПланСчетов.Хозрасчетный.ПустаяСсылка)
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	ВременнаяТаблицаПары.Счет
	|ПОМЕСТИТЬ ВременнаяТаблицаСчета
	|ИЗ
	|	ВременнаяТаблицаПары КАК ВременнаяТаблицаПары
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	ВременнаяТаблицаПары.ПарныйСчет
	|ИЗ
	|	ВременнаяТаблицаПары КАК ВременнаяТаблицаПары
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ОСВ.Субконто1 КАК Контрагент,
	|	ОСВ.Субконто2 КАК Договор,
	|	ОСВ.Счет КАК Счет,
	|	ОСВ.СуммаОборотДт КАК Дебит,
	|	ОСВ.СуммаОборотКт КАК Кредит,
	|	ОСВ.СуммаНачальныйОстатокДт - ОСВ.СуммаНачальныйОстатокКт КАК СальдоН,
	|	ОСВ.СуммаКонечныйОстатокДт - ОСВ.СуммаКонечныйОстатокКт КАК СальдоК,
	|	ВременнаяТаблицаПарыП.Счет КАК Счет1,
	|	ВременнаяТаблицаПарыП.ПарныйСчет,
	|	ОСВ.Счет КАК Счет2,
	|	ОСВ.Субконто1,
	|	ОСВ.Субконто2,
	|	ОСВ.Субконто3,
	|	ОСВ.Организация,
	|	ОСВ.Валюта
	|ИЗ
	|	РегистрБухгалтерии.Хозрасчетный.ОстаткиИОбороты(
	|			&НачПериода,
	|			&КонПериода,
	|			,
	|			,
	|			Счет В
	|				(ВЫБРАТЬ
	|					ВременнаяТаблицаСчета.Счет
	|				ИЗ
	|					ВременнаяТаблицаСчета КАК ВременнаяТаблицаСчета),
	|			&ВидыСубконто,
	|			) КАК ОСВ
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВременнаяТаблицаПары КАК ВременнаяТаблицаПары
	|		ПО ОСВ.Счет = ВременнаяТаблицаПары.Счет
	|			И ОСВ.Счет = ВременнаяТаблицаПары.ПарныйСчет,
	|	ВременнаяТаблицаПары КАК ВременнаяТаблицаПарыП
	|
	|УПОРЯДОЧИТЬ ПО
	|	Контрагент,
	|	Договор,
	|	Счет
	|АВТОУПОРЯДОЧИВАНИЕ";
	
	Запрос.УстановитьПараметр("НачПериода", НачПериода);
	Запрос.УстановитьПараметр("КонПериода", КонецДня(КонПериода));
	Запрос.УстановитьПараметр("ВидыСубконто", ВидыСубконто);
	
	ТЗ = Запрос.Выполнить().Выгрузить();
		
	Объект.ОСВК.Загрузить(ТЗ);
	
	//Для Каждого СТЧ Из Объект.ОСВК Цикл
	//	
	//	СТЧ.СальдоНДт 	= СТЧ.СальдоНДт - СТЧ.СальдоНКт;
	//	СТЧ.СальдоНКт 	= 0;
	//	Если СТЧ.СальдоНДт < 0 Тогда 
	//		СТЧ.СальдоНКт 	= - СТЧ.СальдоНДт;
	//		СТЧ.СальдоНДт 	= 0;
	//	КонецЕсли;			
	//	
	//	СТЧ.СальдоКДт 	= СТЧ.СальдоНДт - СТЧ.СальдоНКт + СТЧ.Дебит - СТЧ.Кредит;
	//	СТЧ.СальдоККт 	= 0;
	//	Если СТЧ.СальдоКДт < 0 Тогда 
	//		СТЧ.СальдоККт 	= - СТЧ.СальдоКДт;
	//		СТЧ.СальдоКДт 	= 0;
	//	КонецЕсли;			
	//	
	//КонецЦикла;
	
	
КонецПроцедуры

&НаСервере
Процедура СформироватьОСВКПоКДНаСервере()
	
	ВидыСубконто = Новый СписокЗначений;
	ВидыСубконто.Добавить(ПланыВидовХарактеристик.ВидыСубконтоХозрасчетные.Контрагенты);
	ВидыСубконто.Добавить(ПланыВидовХарактеристик.ВидыСубконтоХозрасчетные.Договоры);
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ОСВ.Субконто1 КАК Контрагент,
	|	ОСВ.Субконто2 КАК Договор,
	|	ОСВ.Счет КАК Счет,
	|	ОСВ.СуммаОборотДт КАК Дебит,
	|	ОСВ.СуммаОборотКт КАК Кредит,
	|	ОСВ.СуммаНачальныйОстатокДт - ОСВ.СуммаНачальныйОстатокКт КАК СальдоН,
	|	ОСВ.СуммаКонечныйОстатокДт - ОСВ.СуммаКонечныйОстатокКт КАК СальдоК
	|ИЗ
	|	РегистрБухгалтерии.Хозрасчетный.ОстаткиИОбороты(
	|			&НачПериода,
	|			&КонПериода,
	|			,
	|			,
	|			Счет = &Счет
	|					И &Счет <> ЗНАЧЕНИЕ(ПланСчетов.Хозрасчетный.ПустаяСсылка)
	|				ИЛИ &Счет = ЗНАЧЕНИЕ(ПланСчетов.Хозрасчетный.ПустаяСсылка)
	|					И Счет <> ЗНАЧЕНИЕ(ПланСчетов.Хозрасчетный.НДСНаАвансы),
	|			&ВидыСубконто,
	|			Субконто1 = &Контрагент
	|				И (Субконто2 = &Договор
	|					ИЛИ &Договор = ЗНАЧЕНИЕ(Справочник.ДоговорыКонтрагентов.ПустаяСсылка))) КАК ОСВ
	|
	|УПОРЯДОЧИТЬ ПО
	|	Контрагент,
	|	Договор,
	|	Счет
	|АВТОУПОРЯДОЧИВАНИЕ";
	
	Запрос.УстановитьПараметр("НачПериода", 	НачПериода);
	Запрос.УстановитьПараметр("КонПериода", 	КонецДня(КонПериода));
	Запрос.УстановитьПараметр("ВидыСубконто", 	ВидыСубконто);
	Запрос.УстановитьПараметр("Контрагент", 	Контрагент);
	Запрос.УстановитьПараметр("Договор", 		Договор);
	Запрос.УстановитьПараметр("Счет",	 		Счет);
	
	ТЗ = Запрос.Выполнить().Выгрузить();
		
	Объект.ОСВК.Загрузить(ТЗ);
	
	//Для Каждого СТЧ Из Объект.ОСВК Цикл
	//	
	//	СТЧ.СальдоНДт 	= СТЧ.СальдоНДт - СТЧ.СальдоНКт;
	//	СТЧ.СальдоНКт 	= 0;
	//	Если СТЧ.СальдоНДт < 0 Тогда 
	//		СТЧ.СальдоНКт 	= - СТЧ.СальдоНДт;
	//		СТЧ.СальдоНДт 	= 0;
	//	КонецЕсли;			
	//	
	//	СТЧ.СальдоКДт 	= СТЧ.СальдоНДт - СТЧ.СальдоНКт + СТЧ.Дебит - СТЧ.Кредит;
	//	СТЧ.СальдоККт 	= 0;
	//	Если СТЧ.СальдоКДт < 0 Тогда 
	//		СТЧ.СальдоККт 	= - СТЧ.СальдоКДт;
	//		СТЧ.СальдоКДт 	= 0;
	//	КонецЕсли;			
	//	
	//КонецЦикла;
	
	
КонецПроцедуры

&НаСервере
Процедура СформироватьОперацииНаСервере()
	
	ВидыСубконто = Новый СписокЗначений;
	ВидыСубконто.Добавить(ПланыВидовХарактеристик.ВидыСубконтоХозрасчетные.Контрагенты);
	ВидыСубконто.Добавить(ПланыВидовХарактеристик.ВидыСубконтоХозрасчетные.Договоры);
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ХозОбороты.Период КАК Дата,
	|	ХозОбороты.Регистратор КАК Документ,
	|	ХозОбороты.СуммаОборотДт КАК Дебит,
	|	ХозОбороты.СуммаОборотКт КАК Кредит
	|ИЗ
	|	РегистрБухгалтерии.Хозрасчетный.Обороты(
	|			&НачПериода,
	|			&КонПериода,
	|			Регистратор,
	|			Счет <> ЗНАЧЕНИЕ(ПланСчетов.Хозрасчетный.НДСНаАвансы),
	|			&ВидыСубконто,
	|			Субконто1 = &Контрагент
	|				И (Субконто2 = &Договор
	|					ИЛИ &Договор = ЗНАЧЕНИЕ(Справочник.ДоговорыКонтрагентов.ПустаяСсылка)), 
	|			,
	|			) КАК ХозОбороты
	|
	|УПОРЯДОЧИТЬ ПО
	|	Дата,
	|	Документ
	|АВТОУПОРЯДОЧИВАНИЕ";
	
	Запрос.УстановитьПараметр("НачПериода", 	НачПериода);
	Запрос.УстановитьПараметр("КонПериода", 	КонецДня(КонПериода));
	Запрос.УстановитьПараметр("ВидыСубконто", 	ВидыСубконто);	
	Запрос.УстановитьПараметр("Контрагент", 	Контрагент);
	Запрос.УстановитьПараметр("Договор", 		Договор);
	
	ТЗ = Запрос.Выполнить().Выгрузить();
	
	Объект.Операции.Загрузить(ТЗ);
		
	
КонецПроцедуры

&НаСервере
Процедура СформироватьОСВ1549НаСервере()
	
	ВидыСубконто = Новый СписокЗначений;
	ВидыСубконто.Добавить(ПланыВидовХарактеристик.ВидыСубконтоХозрасчетные.Контрагенты);
	ВидыСубконто.Добавить(ПланыВидовХарактеристик.ВидыСубконтоХозрасчетные.Договоры);
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ОСВ.СуммаОборотДт КАК Дебит,
	|	ОСВ.СуммаОборотКт КАК Кредит,
	|	ОСВ.СуммаНачальныйОстатокДт - ОСВ.СуммаНачальныйОстатокКт КАК СальдоН,
	|	ОСВ.СуммаКонечныйОстатокДт - ОСВ.СуммаКонечныйОстатокКт КАК СальдоК
	|ИЗ
	|	РегистрБухгалтерии.Хозрасчетный.ОстаткиИОбороты(
	|			&НачПериода,
	|			&КонПериода,
	|			,
	|			,
	|			Счет = ЗНАЧЕНИЕ(ПланСчетов.Хозрасчетный.НДСНаАвансы),
	|			&ВидыСубконто,
	|			Субконто1 = &Контрагент
	|				И Субконто2 = &Договор) КАК ОСВ";
	
	Запрос.УстановитьПараметр("НачПериода", НачПериода);
	Запрос.УстановитьПараметр("КонПериода", КонецДня(КонПериода));
	Запрос.УстановитьПараметр("ВидыСубконто", ВидыСубконто);
	Запрос.УстановитьПараметр("Контрагент", 	Контрагент);
	Запрос.УстановитьПараметр("Договор", 		Договор);
	
	ТЗ = Запрос.Выполнить().Выгрузить();
		
	Объект.ОСВ1549.Загрузить(ТЗ);
	
	
КонецПроцедуры

&НаСервере
Процедура СформироватьАвансыНаСервере()
	
	ВидыСубконто = Новый СписокЗначений;
	ВидыСубконто.Добавить(ПланыВидовХарактеристик.ВидыСубконтоХозрасчетные.Контрагенты);
	ВидыСубконто.Добавить(ПланыВидовХарактеристик.ВидыСубконтоХозрасчетные.Договоры);
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ХозОбороты.Период КАК Дата,
	|	ХозОбороты.Регистратор КАК Документ,
	|	ХозОбороты.СуммаОборотДт КАК Дебит,
	|	ХозОбороты.СуммаОборотКт КАК Кредит,
	|	ХозОбороты.Субконто1,
	|	ХозОбороты.Субконто2
	|ПОМЕСТИТЬ ВременнаяТаблицаОплата
	|ИЗ
	|	РегистрБухгалтерии.Хозрасчетный.Обороты(
	|			&НачПериода,
	|			&КонПериода,
	|			Регистратор,
	|			Счет <> ЗНАЧЕНИЕ(ПланСчетов.Хозрасчетный.НДСНаАвансы),
	|			&ВидыСубконто,
	|			Субконто1 = &Контрагент
	|				И Субконто2 = &Договор,
	|			,
	|			) КАК ХозОбороты
	|ГДЕ
	|	ХозОбороты.СуммаОборотКт <> 0
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	АИД.ДокументА КАК ДокАванса,
	|	АИД.Сумма,
	|	АИД.ВидДвижения,
	|	АИД.СуммаНДС,
	|	АИД.СуммаНСП,
	|	АИД.Регистратор,
	|	АИД.Контрагент,
	|	АИД.Договор
	|ПОМЕСТИТЬ ВременнаяТаблицаАвансы
	|ИЗ
	|	РегистрНакопления.АвансыДоотгрузка КАК АИД
	|ГДЕ
	|	АИД.Период МЕЖДУ &НачПериода И &КонПериода
	|	И АИД.Договор = &Договор
	|	И АИД.Контрагент = &Контрагент
	|	И АИД.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Приход)
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВременнаяТаблицаОплата.Дата КАК Дата,
	|	ВременнаяТаблицаОплата.Документ КАК Документ,
	|	ВременнаяТаблицаОплата.Кредит КАК СуммаОп,
	|	ВременнаяТаблицаАвансы.Сумма КАК СуммаАванса,
	|	ВременнаяТаблицаАвансы.СуммаНДС КАК НДС,
	|	ВременнаяТаблицаАвансы.СуммаНСП КАК НСП
	|ИЗ
	|	ВременнаяТаблицаОплата КАК ВременнаяТаблицаОплата
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВременнаяТаблицаАвансы КАК ВременнаяТаблицаАвансы
	|		ПО ВременнаяТаблицаОплата.Документ = ВременнаяТаблицаАвансы.ДокАванса
	|
	|УПОРЯДОЧИТЬ ПО
	|	Дата,
	|	Документ
	|АВТОУПОРЯДОЧИВАНИЕ";

	
	Запрос.УстановитьПараметр("НачПериода", 	НачПериода);
	Запрос.УстановитьПараметр("КонПериода", 	КонецДня(КонПериода));
	Запрос.УстановитьПараметр("ВидыСубконто", 	ВидыСубконто);
	Запрос.УстановитьПараметр("Контрагент", 	Контрагент);
	Запрос.УстановитьПараметр("Договор", 		Договор);
	
	ТЗ = Запрос.Выполнить().Выгрузить();
		
	Объект.Авансы.Загрузить(ТЗ);
	
	
КонецПроцедуры

&НаСервере
Процедура СформироватьДоотгрузкуНаСервере()
	
	ВидыСубконто = Новый СписокЗначений;
	ВидыСубконто.Добавить(ПланыВидовХарактеристик.ВидыСубконтоХозрасчетные.Контрагенты);
	ВидыСубконто.Добавить(ПланыВидовХарактеристик.ВидыСубконтоХозрасчетные.Договоры);
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ХозОбороты.Период КАК Дата,
	|	ХозОбороты.Регистратор КАК ДокОтгрузки,
	|	ХозОбороты.СуммаОборотДт КАК Дебит,
	|	ХозОбороты.СуммаОборотКт КАК Кредит,
	|	ХозОбороты.Субконто1,
	|	ХозОбороты.Субконто2
	|ПОМЕСТИТЬ ВременнаяТаблицаОтгрузка
	|ИЗ
	|	РегистрБухгалтерии.Хозрасчетный.Обороты(
	|			&НачПериода,
	|			&КонПериода,
	|			Регистратор,
	|			Счет <> ЗНАЧЕНИЕ(ПланСчетов.Хозрасчетный.НДСНаАвансы),
	|			&ВидыСубконто,
	|			Субконто1 = &Контрагент
	|				И Субконто2 = &Договор,
	|			,
	|			) КАК ХозОбороты
	|ГДЕ
	|	ХозОбороты.СуммаОборотДт <> 0
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	АИД.ДокументА КАК ДокАванса,
	|	АИД.Сумма,
	|	АИД.ВидДвижения,
	|	АИД.СуммаНДС,
	|	АИД.СуммаНСП,
	|	АИД.Регистратор,
	|	АИД.Контрагент,
	|	АИД.Договор,
	|	АИД.ДокументОтгр
	|ПОМЕСТИТЬ ВременнаяТаблицаДоотгрузка
	|ИЗ
	|	РегистрНакопления.АвансыДоотгрузка КАК АИД
	|ГДЕ
	|	АИД.Период МЕЖДУ &НачПериода И &КонПериода
	|	И АИД.Договор = &Договор
	|	И АИД.Контрагент = &Контрагент
	|	И АИД.ВидДвижения = ЗНАЧЕНИЕ(ВидДвиженияНакопления.Расход)
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТОотгрузка.Дата КАК Дата,
	|	ВТОотгрузка.ДокОтгрузки КАК ДокОтгрузки,
	|	ВТОотгрузка.Дебит КАК СуммаОп,
	|	ВременнаяТаблицаДоотгрузка.Сумма КАК СуммаДоотгр,
	|	ВременнаяТаблицаДоотгрузка.СуммаНДС КАК НДС,
	|	ВременнаяТаблицаДоотгрузка.СуммаНСП КАК НСП,
	|	ВременнаяТаблицаДоотгрузка.ДокАванса КАК ДокАванса
	|ИЗ
	|	ВременнаяТаблицаОтгрузка КАК ВТОотгрузка
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВременнаяТаблицаДоотгрузка КАК ВременнаяТаблицаДоотгрузка
	|		ПО ВТОотгрузка.ДокОтгрузки = ВременнаяТаблицаДоотгрузка.ДокументОтгр
	|
	|УПОРЯДОЧИТЬ ПО
	|	Дата,
	|	ДокАванса
	|АВТОУПОРЯДОЧИВАНИЕ";

	
	Запрос.УстановитьПараметр("НачПериода", 	НачПериода);
	Запрос.УстановитьПараметр("КонПериода", 	КонецДня(КонПериода));
	Запрос.УстановитьПараметр("ВидыСубконто", 	ВидыСубконто);
	Запрос.УстановитьПараметр("Контрагент", 	Контрагент);
	Запрос.УстановитьПараметр("Договор", 		Договор);
	
	ТЗ = Запрос.Выполнить().Выгрузить();
		
	Объект.Доотгрузка.Загрузить(ТЗ);
	
	
КонецПроцедуры

#КонецОбласти



