#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область СлужебныйПрограммныйИнтерфейс

// Функция возвращет массив ссылок обработанных объектов 
//
// Процедура - Заполнить по умолчанию
// Выполняет первоночальное заполнение
Процедура ЗаполнитьПоУмолчанию() Экспорт 
	МассивЭлементов = Новый Массив; 	
	СправочникМенеджер = ПланыВидовРасчета.ВидыНалогов;
	
	КлассификаторXML = СправочникМенеджер.ПолучитьМакет("Макет").ПолучитьТекст();
	КлассификаторТаблица = ОбщегоНазначения.ПрочитатьXMLВТаблицу(КлассификаторXML).Данные;     
	
	Для Каждого СтрокаТаблицыЗначений Из КлассификаторТаблица Цикл
		Наименование 				= СокрЛП(СтрокаТаблицыЗначений.Наименование);
		ВидНалога 					= СокрЛП(СтрокаТаблицыЗначений.ВидНалога);
		ФормулаРасчета 				= СокрЛП(СтрокаТаблицыЗначений.ФормулаРасчета);
		РеквизитДопУпорядочивания 	= СокрЛП(СтрокаТаблицыЗначений.РеквизитДопУпорядочивания);
		Описание 					= СокрЛП(СтрокаТаблицыЗначений.Описание);
		
		СправочникСсылка = СправочникМенеджер.НайтиПоНаименованию(Наименование, Истина);
		
		Если СправочникСсылка.Пустая() Тогда
			СправочникОбъект = СправочникМенеджер.СоздатьВидРасчета();
		Иначе
			СправочникОбъект = СправочникСсылка.ПолучитьОбъект();
		КонецЕсли;	
		
		СправочникОбъект.Наименование = Наименование;
		СправочникОбъект.ВидНалога = Перечисления.ВидыНалоговПоЗаработнойПлате[ВидНалога];
		СправочникОбъект.ФормулаРасчета = ФормулаРасчета;
		СправочникОбъект.РеквизитДопУпорядочивания = РеквизитДопУпорядочивания;
		СправочникОбъект.Описание = Описание;
		
		Попытка
			СправочникОбъект.Записать();
		Исключение
		КонецПопытки;  
	КонецЦикла;
КонецПроцедуры // ЗаполнитьПоУмолчанию()	

#КонецОбласти
	
#КонецЕсли



