#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

// Процедура создает пустую временную таблицу изменения движений.
//
Процедура СоздатьПустуюВременнуюТаблицуИзменение(ДополнительныеСвойства) Экспорт
	
	Если НЕ ДополнительныеСвойства.Свойство("ДляПроведения")
	 ИЛИ НЕ ДополнительныеСвойства.ДляПроведения.Свойство("СтруктураВременныеТаблицы") Тогда
		Возврат;
	КонецЕсли;
	
	СтруктураВременныеТаблицы = ДополнительныеСвойства.ДляПроведения.СтруктураВременныеТаблицы;
	
	Запрос = Новый Запрос(
	"ВЫБРАТЬ ПЕРВЫЕ 0
	|	МБПЭксплуатация.НомерСтроки КАК НомерСтроки,
	|	МБПЭксплуатация.Организация КАК Организация,
	|	МБПЭксплуатация.Подразделение,
	|	МБПЭксплуатация.МОЛ,
	|	МБПЭксплуатация.Номенклатура КАК Номенклатура,
	|	МБПЭксплуатация.ИнвНомер,
	|	МБПЭксплуатация.Партия КАК Партия,
	|	МБПЭксплуатация.ДатаВыдачи,
	|	МБПЭксплуатация.Количество КАК КоличествоПередЗаписью,
	|	МБПЭксплуатация.Количество КАК КоличествоИзменение,
	|	МБПЭксплуатация.Количество КАК КоличествоПриЗаписи,
	|	МБПЭксплуатация.Сумма КАК СуммаПередЗаписью,
	|	МБПЭксплуатация.Сумма КАК СуммаИзменение,
	|	МБПЭксплуатация.Сумма КАК СуммаПриЗаписи
	|ПОМЕСТИТЬ ДвиженияМБПЭксплуатацияИзменение
	|ИЗ
	|	РегистрНакопления.МБПЭксплуатация КАК МБПЭксплуатация");
	
	Запрос.МенеджерВременныхТаблиц = СтруктураВременныеТаблицы.МенеджерВременныхТаблиц;
	РезультатЗапроса = Запрос.Выполнить();
	
	СтруктураВременныеТаблицы.Вставить("ДвиженияМБПЭксплуатацияИзменение", Ложь);
	
КонецПроцедуры // СоздатьПустуюВременнуюТаблицуИзменение()

#КонецОбласти

#КонецЕсли