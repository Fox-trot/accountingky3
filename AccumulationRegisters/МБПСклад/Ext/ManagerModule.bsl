#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

// Процедура создает пустую временную таблицу изменения движений.
//
Процедура СоздатьПустуюВременнуюТаблицуИзменение(ДополнительныеСвойства) Экспорт
	
	Если НЕ ДополнительныеСвойства.Свойство("ДляПроведения")
	 ИЛИ НЕ ДополнительныеСвойства.ДляПроведения.Свойство("СтруктураВременныеТаблицы") Тогда
		Возврат;
	КонецЕсли;
	
	СтруктураВременныеТаблицы = ДополнительныеСвойства.ДляПроведения.СтруктураВременныеТаблицы;
	
	Запрос = Новый Запрос(
	"ВЫБРАТЬ ПЕРВЫЕ 0
	|	МБПСклад.НомерСтроки КАК НомерСтроки,
	|	МБПСклад.Организация КАК Организация,
	|	МБПСклад.Склад КАК Склад,
	|	МБПСклад.Номенклатура КАК Номенклатура,
	|	МБПСклад.ИнвНомер,
	|	МБПСклад.Статус,
	|	МБПСклад.Партия КАК Партия,
	|	МБПСклад.Количество КАК КоличествоПередЗаписью,
	|	МБПСклад.Количество КАК КоличествоИзменение,
	|	МБПСклад.Количество КАК КоличествоПриЗаписи,
	|	МБПСклад.Сумма КАК СуммаПередЗаписью,
	|	МБПСклад.Сумма КАК СуммаИзменение,
	|	МБПСклад.Сумма КАК СуммаПриЗаписи
	|ПОМЕСТИТЬ ДвиженияМБПСкладИзменение
	|ИЗ
	|	РегистрНакопления.МБПСклад КАК МБПСклад");
	
	Запрос.МенеджерВременныхТаблиц = СтруктураВременныеТаблицы.МенеджерВременныхТаблиц;
	РезультатЗапроса = Запрос.Выполнить();
	
	СтруктураВременныеТаблицы.Вставить("ДвиженияМБПСкладИзменение", Ложь);
	
КонецПроцедуры // СоздатьПустуюВременнуюТаблицуИзменение()

#КонецОбласти

#КонецЕсли